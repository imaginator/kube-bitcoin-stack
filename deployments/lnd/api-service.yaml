apiVersion: v1
kind: Service
metadata:
  name: lnd
  namespace: lnd
  labels:
    app.kubernetes.io/name: lnd
    app.kubernetes.io/part-of: kube-bitcoin-stack
spec:
  type: ClusterIP
  ports:
    - name: rpc
      port: 10009
    - name: rest
      port: 8080
  selector:
    app.kubernetes.io/name: lnd
